# Luma 界面验证

final result: passed

## 对照范围与证据

- source visual truth：本会话用户提供的 Launchpad 参考图（原图 3024 × 1964，展示尺寸 1971 × 1280），以及后续“移除 Dock、移除页内 Luma、加速图标、横向翻页、四组快捷键”的明确修订。
- implementation screenshot：`.preview/launchpad-final.jpg`（原生 macOS Tauri 窗口）。
- settings screenshot：`.preview/settings-final.jpg`。
- 窗口配置为 1200 × 800；CUA 输出为 1152 × 768 JPEG（含系统标题栏，整体缩放 0.96）。
- 主画面状态：138 个真实应用，8 列 × 4 行，第一页；正常浅色背景，未显示 Dock 或页内品牌文字。
- 参考图与实现截图在本次视觉审阅上下文中对照。窗口比例、应用数量和内容来自当前机器，未宣称进行同尺寸逐像素复刻或图片拼接。
- 主画面全图中可辨认图标、名称、间距、搜索及分页；设置单独截图作为控件和文字的局部验证。截图保存在 git 忽略的 `.preview/` 中。

## 比较与修订记录

1. 初稿采用参考图中的 Dock，并在左上角加入品牌文字。用户明确要求删除后，已移除组件、收藏逻辑及相关样式；最终截图确认只保留搜索、网格、右上角操作和底部分页。
2. 初稿按页卸载网格并淡入，图标会等待异步读取。现在页面保持挂载，使用 420ms 横向 transform 过渡；预加载相邻页、提前解码图片、命中内存和磁盘缓存。
3. 初稿原生图标导出全部 TIFF 分辨率，成本过高。现在按图标尺寸读取单张 PNG；本机导出的 138 张均为 256 × 256。
4. 调亮背景、收紧常规窗口行距，避免初稿 1200 × 800 窗口中过于松散的三行布局。最终为四行，图标与两行名称均完整显示。
5. 搜索结果变化和窗口排布变化独立重建页面轨道，避免搜索时横向穿过已不存在的页；普通翻页不重建图标。

## 五项视觉检查

- 字体：使用系统字体，应用名 12–13px，两行截断；中文与长名称可读，控件保留键盘焦点。
- 间距：搜索居中；图标直接位于背景上，等距网格；分页固定在底部，无 Dock 占位。
- 颜色：奶白、低饱和蓝、暖褐与灰橄榄背景；搜索与设置使用浅色半透明表面。
- 图像：macOS 图标来自系统接口，使用 PNG 与预解码；背景为独立生成的本地图像。无手工模拟品牌图标。
- 内容：显示当前机器的应用名称；设置说明与实际行为一致；页内品牌文字按要求删除。

## 功能与性能证据

- 原生 `←`：第 3 页切到第 2 页；`→`：第 2 页切回第 3 页。
- 原生鼠标纵向滚动：第 1 页进入第 2 页。
- 原生 `⌘,`：打开设置对话框，截图确认显示背景模糊、减少动态效果、图标缓存及快捷键说明。
- 搜索 `Calculator` 得到一个结果；按 Enter 后观察到计算器进程运行。
- 原生 Esc：CUA 返回 `App quit`，进程检查确认 Luma 退出；随后重新打开并留在第一页。
- Rust 6 个测试通过，包括磁盘缓存跨实例复用、失效和安全清理范围；Clippy 无警告。
- 前端构建及 macOS 调试应用打包通过。Windows/Linux 平台和缓存模块通过交叉编译与 Clippy。
- 本机缓存微基准（138 张图标）：首次生成约 1.105s；内存命中约 1.83ms；新缓存实例从磁盘读取约 8.34ms。此数值不包含 IPC、浏览器解码或界面渲染，不等同于整屏启动时间。

## 验证边界

- 未进行真实触控板硬件的帧率测量；CUA 横向滚动模拟未得到可确认的翻页结果。横向手势跟随和结束吸附已实现，键盘和鼠标纵向翻页已验证。
- 未在 Windows/Linux 桌面环境实测原生图标和界面；缺少本地图标的入口保留通用图标。
- 未捕获 WebView 控制台日志。原生渲染、搜索、设置、IPC 启动和关闭流程未出现可见错误。

## 结论

无当前目标范围内待修复的 P0/P1/P2 视觉问题。后续可根据真实触控板使用反馈微调手势阈值和缓动。
